<UserControl x:Class="QRCodeManager.WPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Border Style="{StaticResource DashboardCard}"
                MaxWidth="420"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="28">
            <StackPanel>
                <TextBlock Text="Giriş Yap"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TitleTextBrush}" />
                <TextBlock Text="Hesabınızla giriş yapın veya devam etmek için atlayın."
                           Margin="0,8,0,24"
                           Foreground="{DynamicResource MutedTextBrush}"
                           TextWrapping="Wrap" />

                <TextBox Margin="0,0,0,16"
                         materialDesign:HintAssist.Hint="E-posta"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

                <PasswordBox x:Name="PasswordBox"
                             Margin="0,0,0,20"
                             materialDesign:HintAssist.Hint="Şifre"
                             Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                             PasswordChanged="OnPasswordChanged" />

                <Button Content="Giriş Yap"
                        Margin="0,0,0,8"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding LoginCommand}" />

                <Button Content="Kayıt Ol"
                        Margin="0,0,0,8"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding GoToRegisterCommand}" />

                <Button Content="Atla ve Devam Et"
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Command="{Binding SkipCommand}" />

                <TextBlock Text="{Binding StatusMessage}"
                           Margin="0,12,0,0"
                           Foreground="{DynamicResource MutedTextBrush}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
